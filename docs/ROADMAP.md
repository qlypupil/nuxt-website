# 项目优化路线图

本文档记录项目的优化建议与待办事项，按优先级和类别整理，供团队参考和跟进。

---

## 优先级说明

- 🔴 **高优先级** - 影响核心功能或用户体验，建议尽快完成
- 🟡 **中优先级** - 提升代码质量或开发效率，建议近期完成
- 🟢 **低优先级** - 锦上添花，按需完成

---

## 一、项目结构与架构

| 优先级 | 任务                     | 说明                                                                 | 状态    |
| ------ | ------------------------ | -------------------------------------------------------------------- | ------- |
| 🟡     | 封装 `useApi` composable | 统一封装 `$fetch`/`useFetch`，包含错误处理、loading 状态、token 注入 | ⬜ 待办 |
| 🟡     | 添加 `app/types/` 目录   | 存放接口响应类型、业务实体类型定义                                   | ⬜ 待办 |
| 🟢     | 添加 `app/utils/` 目录   | 存放工具函数（日期格式化、字符串处理等）                             | ⬜ 待办 |
| 🟢     | 添加 `app/middleware/`   | 预留路由中间件目录（鉴权等）                                         | ⬜ 待办 |
| 🟢     | 添加 `app/plugins/`      | 预留插件目录（第三方库注册等）                                       | ⬜ 待办 |

---

## 二、性能优化

| 优先级 | 任务                               | 说明                                                                       | 状态    |
| ------ | ---------------------------------- | -------------------------------------------------------------------------- | ------- |
| 🟡     | 使用 `<NuxtImg>` / `<NuxtPicture>` | 已引入 `@nuxt/image`，但未使用。图片应自动懒加载、WebP 转换、响应式 srcset | ⬜ 待办 |
| 🟢     | 字体优化                           | 如使用自定义字体，用 `@nuxt/fonts` 或手动 preload + `font-display: swap`   | ⬜ 待办 |
| 🟢     | Tailwind 未来特性                  | 考虑开启 `future: { hoverOnlyWhenSupported: true }`                        | ⬜ 待办 |
| 🟢     | 考虑 SSG 部署                      | 官网内容相对静态，可用 `nuxt generate` 生成纯静态站点配合 CDN              | ⬜ 待办 |
| 🟢     | 组件懒加载                         | 大型组件用 `<LazyXxx />` 或 `defineAsyncComponent` 按需加载                | ⬜ 待办 |
| 🟢     | 第三方脚本异步加载                 | 统计/客服脚本用 `useScript` (Nuxt Scripts) 异步加载                        | ⬜ 待办 |

---

## 三、SEO 优化

| 优先级 | 任务                   | 说明                                                                                            | 状态    |
| ------ | ---------------------- | ----------------------------------------------------------------------------------------------- | ------- |
| 🔴     | 添加 Sitemap           | 引入 `@nuxtjs/sitemap` 自动生成 sitemap.xml                                                     | ⬜ 待办 |
| 🔴     | 完善 robots.txt        | 补充 `Sitemap:` 指令指向 sitemap.xml                                                            | ⬜ 待办 |
| 🟡     | 添加 Open Graph 元数据 | 各页面 `useSeoMeta` 补充 `ogTitle`、`ogDescription`、`ogImage`、`twitterCard`（见下方名词解释） | ⬜ 待办 |
| 🟡     | 修复 app.head 硬编码   | `nuxt.config.ts` 中 `title` 和 `description` 是硬编码中文，应移除或改为默认语言翻译             | ⬜ 待办 |
| 🟢     | 添加结构化数据         | 加入 `Organization`、`WebSite`、`BreadcrumbList` 等 JSON-LD                                     | ⬜ 待办 |
| 🟢     | 验证 Canonical URL     | 确认 `useLocaleHead` 生成的 canonical 是否正确                                                  | ⬜ 待办 |

### 名词解释：Open Graph 元数据

- **Open Graph（OG）**：由 Facebook 提出的一套 meta 标签协议，用于在**社交平台分享链接时**展示「链接预览卡片」（标题、描述、缩略图等）。
- **`og:title`（ogTitle）**：分享卡片上显示的**标题**，一般与页面 title 一致或略短。
- **`og:description`（ogDescription）**：分享卡片上的**摘要文案**，建议与页面 description 一致。
- **`og:image`（ogImage）**：分享卡片的**主图**，需为绝对 URL，推荐尺寸约 1200×630，格式 JPG/PNG。
- **`twitter:card`（twitterCard）**：在 Twitter/X 上的卡片样式，常用 `summary_large_image` 表示「大图 + 标题 + 描述」。

在 Nuxt 里通过 `useSeoMeta({ ogTitle, ogDescription, ogImage, twitterCard, ... })` 设置后，分享到微信、微博、Facebook、Twitter、LinkedIn 等时，会按这些字段生成预览，提升点击率与品牌一致性。

---

## 四、安全性

| 优先级 | 任务        | 说明                                                               | 状态    |
| ------ | ----------- | ------------------------------------------------------------------ | ------- |
| 🟡     | 配置安全头  | 添加 CSP、X-Frame-Options 等，通过 Nitro routeRules 或部署平台配置 | ⬜ 待办 |
| 🟢     | CI 依赖审计 | 在 CI 中加入 `npm audit --audit-level=high` 检查依赖漏洞           | ⬜ 待办 |

---

## 五、开发体验（DX）

| 优先级 | 任务                 | 说明                                                         | 状态      |
| ------ | -------------------- | ------------------------------------------------------------ | --------- |
| ✅     | 重写 README          | 包含项目简介、技术栈、环境变量、开发步骤、部署方式、目录结构 | ✅ 已完成 |
| ✅     | 补充 .env.example    | 填写所有需要的环境变量占位                                   | ✅ 已完成 |
| ✅     | 推荐 i18n-ally 扩展  | 在 extensions.json 中添加 `lokalise.i18n-ally`               | ✅ 已完成 |
| 🟢     | 添加 VSCode 调试配置 | 创建 `.vscode/launch.json`，配置 Node 调试和浏览器调试       | ⬜ 待办   |
| 🟢     | 添加 clean 脚本      | `"clean": "rm -rf .nuxt .output node_modules/.cache"`        | ⬜ 待办   |

---

## 六、代码质量

| 优先级 | 任务                   | 说明                                                             | 状态    |
| ------ | ---------------------- | ---------------------------------------------------------------- | ------- |
| 🔴     | AppFooter 版权 i18n    | 版权文字「公司名称」是硬编码中文，应改为 i18n 翻译               | ⬜ 待办 |
| 🟡     | 加强 TypeScript 严格度 | tsconfig 追加 `"strict": true, "noUncheckedIndexedAccess": true` | ⬜ 待办 |
| 🟡     | 引入单元测试           | 添加 `@nuxt/test-utils` + `vitest`，覆盖 composables 和工具函数  | ⬜ 待办 |
| 🟢     | 增强 ESLint 规则       | 考虑启用 `@nuxt/eslint` 的 `stylistic` 选项或 `vue3-recommended` | ⬜ 待办 |
| 🟢     | 引入 E2E 测试          | 用 Playwright 测试核心流程（页面可访问、导航、语言切换）         | ⬜ 待办 |

---

## 七、部署与运维

| 优先级 | 任务            | 说明                                                              | 状态    |
| ------ | --------------- | ----------------------------------------------------------------- | ------- |
| 🟡     | CI 添加部署步骤 | 当前 CI 只做检查，缺少自动部署（Vercel/Netlify/Cloudflare Pages） | ⬜ 待办 |
| 🟢     | 提供 Dockerfile | 如需私有化部署，提供 `Dockerfile` 和 `docker-compose.yml`         | ⬜ 待办 |
| 🟢     | 环境区分        | 考虑 `.env.development`、`.env.production`、`.env.staging`        | ⬜ 待办 |
| 🟢     | 接入错误监控    | 生产环境接入 Sentry（`@sentry/nuxt`）或类似服务                   | ⬜ 待办 |
| 🟢     | 接入访问统计    | 接入 Google Analytics、Plausible 或百度统计                       | ⬜ 待办 |

---

## 八、国际化（i18n）

| 优先级 | 任务            | 说明                                                           | 状态    |
| ------ | --------------- | -------------------------------------------------------------- | ------- |
| 🟡     | 翻译完整性校验  | 用 i18n-ally 或 CI 脚本校验 zh.json 和 en.json 的 key 是否同步 | ⬜ 待办 |
| 🟢     | 日期/数字格式化 | 如有日期、货币，配置 `datetimeFormats`、`numberFormats`        | ⬜ 待办 |

---

## 九、UI / UX

| 优先级 | 任务                | 说明                                                        | 状态    |
| ------ | ------------------- | ----------------------------------------------------------- | ------- |
| 🔴     | 移动端响应式导航    | Header 导航栏在移动端没有汉堡菜单，小屏幕会溢出             | ⬜ 待办 |
| 🔴     | 自定义 404 页面     | 缺少 `[...slug].vue` 或 `error.vue`，404 使用 Nuxt 默认样式 | ⬜ 待办 |
| 🟡     | 添加 Loading 指示器 | 页面切换时添加 `<NuxtLoadingIndicator />`                   | ⬜ 待办 |
| 🟢     | 暗色模式            | 如需支持，引入 `@nuxtjs/color-mode` + Tailwind `dark:` 前缀 | ⬜ 待办 |
| 🟢     | 滚动行为配置        | 如需保留滚动位置或平滑滚动，配置 `router.options`           | ⬜ 待办 |

---

## 快速参考：高优先级任务清单

以下任务建议优先完成：

- [ ] 🔴 移动端响应式导航（汉堡菜单）
- [ ] 🔴 自定义 404 页面
- [ ] 🔴 AppFooter 版权改为 i18n
- [ ] 🔴 添加 Sitemap
- [ ] 🔴 完善 robots.txt

---

## 更新日志

| 日期       | 更新内容                                                |
| ---------- | ------------------------------------------------------- |
| 2026-01-29 | 初始化优化路线图文档                                    |
| 2026-01-29 | 完成 README 重写、.env.example 补充、i18n-ally 扩展推荐 |

---

## 如何使用本文档

1. **认领任务**：在任务状态栏标注负责人
2. **更新状态**：完成后将 `⬜ 待办` 改为 `✅ 已完成`
3. **新增任务**：发现新的优化点时添加到对应分类
4. **定期回顾**：团队会议时回顾进度，调整优先级
